# Field Focus 개선 로드맵 진행 상황

(Claude Code와 함께 진행 중인 작업 기록. 커밋 히스토리와 같이 보면 상세 내역 확인 가능)

## ⏸ 보류
- Firestore 보안 규칙 (`allow read, write: if true`) — 사용자 요청으로 보류 중, 나중에 다시 논의

## ✅ Phase 0 — 리포트 가독성·정보위계 개선
요약바, 비교강조 바, 지표 툴팁, 접이식 섹션, 화면 폭 제한, 압박맵 축소, 효율표 전환, 턴오버 지도

## ✅ Phase 1 — 대회 · 국가 · 경기 다축 탐색
경기 전환 스위처+사이드바, 대회 카테고리/연도별 재구성, 국가별 보기(팀명 기반, 매핑 불필요) + 카테고리/연도/대회 다중필터, 필터 결과 → 누적분석 연결, 경기명 인라인 수정

## Phase 2 — 리포트 콘텐츠 보강
- ✅ 스탯 클릭 → 이벤트 영상 클립 목록
- ✅ 주요 이벤트 타임라인 (좌우분할+진행스코어, 접기/펼치기, 슈터·막은선수 자동표시)
- ✅ 슈팅 결과 브레이크다운 섹션
- ✅ 모바일 컴팩트 뷰
- ❌ 선수별 분석 (별도 모듈 — 로스터 데이터 모델부터 새로 필요, 가장 큰 작업)

✅ **완료 (2026-08-13): 슈팅 결과 브레이크다운 + 모바일 컴팩트 뷰**
- `src/components/shot-breakdown.tsx` 신규 — `basic-match-stats.tsx`와 동일한 홈/어웨이 스플릿바 UI 재사용. 슈팅+PC 시도를 결과별(득점/GK 선방/블록/PC 유도/빗나감·아웃/포스트/기타/결과 미기록)로 묶어서 표시, 영상 연결 시 숫자 클릭→해당 장면 클립.
  - 태깅 도구 "결과(Output)" 필드는 자유 텍스트라 느슨한 정규식 매칭 사용. 실제 데이터로 확인해보니 결과값이 아예 비어있는 이벤트가 상당수라("결과 미기록") "기타"와 분리— 처음엔 다 "기타"로 뭉쳐서 거의 절반이 분류불가로 나왔던 걸 실 데이터 조회로 잡아냄.
  - 대시보드에서 "종합 통계 비교" 바로 아래에 배치.
- 모바일 뷰(375px) 실측 검증 후 발견된 실제 가로 스크롤 버그들을 수정:
  - 상단 헤더 nav(경기별분석/대회누적분석/대회관리), 업로드 툴바(대회명·경기명·홈어웨이 입력), 매치 스위처(이전/다음 경기), 리포트 제목줄(팀명+비디오/AI버튼), 대회관리 화면 헤더 3곳 — 전부 `flex-wrap` 누락 또는 고정폭(`w-32`/`w-40`/`w-28`) 입력이 원인. `flex-wrap`/`w-full sm:w-*`/`truncate`/`min-w-0`로 수정.
  - `StatsCard`: `Card`에 `min-w-0` 누락으로 grid-cols-2 안에서 텍스트가 잘림(flexbox/grid `min-width:auto` 함정) — `min-w-0` 추가 + 모바일에서 패딩/폰트 축소(`p-3 sm:p-6`, `text-xl sm:text-2xl`).
  - `MatchTrajectoryChart`(공격 점유·속도 분석 산점도): 컨테이너가 항상 `h-[700px]`+고정 마진(60/80/80/60px)이라 모바일에서 극단적으로 좁고 길쭉해져 축 라벨·사분면 라벨이 전부 겹쳐 읽을 수 없었음 — 기존 `useIsMobile()` 훅으로 모바일에서 높이 축소(450px) + 마진/폰트 축소.
  - 넓은 데이터 테이블(예: "25y 진입 대비 서클 진입 효율", 전체 순위표)은 공용 `Table` 컴포넌트가 이미 `overflow-auto` 래퍼를 갖고 있어 자체 가로 스크롤로 격리됨 — 페이지 전체 스크롤엔 영향 없음, 별도 수정 불필요로 확인.
- 실제 서버에서 데스크톱→모바일(375px) 리사이즈로 실제 대회(RSA 네이션스컵) 매치 리포트와 대회 관리 화면을 직접 검증: `document.documentElement.scrollWidth`가 리포트 페이지 497→375, 대회관리 페이지 424→375로 가로 오버플로 완전 해소 확인. 스캐터 차트도 라벨 겹침이 크게 개선됨(완전히 안 겹치진 않음 — 데이터 포인트가 몰린 구간은 산점도 특성상 어쩔 수 없는 부분).

## ✅ Phase 3 — PDF A4 최적화
`@page` 규격, 차트 높이 축소, 폰트 축소, 거대 break-inside-avoid 블록 분리(페이지 절단/공백 버그 수정). 실제 내보낸 PDF로 검증 완료.

## Phase 4 — 데이터·비디오 통합
- ✅ XML 원본 저장 + 재다운로드
- ✅ (부산물) 영상 연결 다이얼로그 — 유튜브 3앵글 URL + stream.json 오프셋 자동입력
- ✅ (부산물) 태깅 도구(`슈팅위치태킹.html`) exportToXML 버그 수정 — GK_Player·커스텀필드 누락 고침
- ✅ 태깅 도구 저장소 편입 (전체병합 export, group 보존)
- ❌ 매치 전체 다운로드 (녹화+XML zip)

✅ **완료 (2026-08-13): 슈팅 태깅 도구 저장소 편입 + 전체병합 export**
- 기존에 저장소 밖(`C:\Users\a\Desktop\shoot\슈팅위치태킹.html`)에 있던 독립 도구를 `public/ShotTagging/index.html`로 편입. 원본 파일은 그대로 두고(수정 안 함), 이제 저장소 안의 사본이 정식 버전. 메인 앱 헤더에 "슈팅 태깅 도구" 버튼 추가(`/ShotTagging/index.html` 새 탭, `Video 분석 도구` 버튼과 동일 패턴).
- **전체병합 export**: 기존엔 타임라인 파일(XML/JSON)을 새로 업로드할 때마다 `instancesData`를 통째로 비우고 시작해서, 경기 여러 개를 순서대로 태깅해도 마지막 파일 것만 남고 이전 건 다 날아갔음 — 여러 경기를 하나로 합쳐서 export할 방법이 아예 없었던 게 핵심 문제였음. 업로드가 누적되도록(초기화 대신 append) 고치고, 새 "전체 병합 XML" 버튼(지금까지 업로드한 모든 경기를 하나의 XML로) 추가. 각 인스턴스가 이미 갖고 있던 `timeline`(출처 파일명) 필드로 병합해도 그룹(row/`<code>`)이 안 섞이고 그대로 보존됨 — 인스턴스 목록에도 2개 이상 파일이 섞였을 때만 작은 출처 배지 표시. "세션 초기화" 버튼으로 명시적으로 리셋 가능(이전엔 새 파일 올리는 게 곧 리셋이었는데 이제 그게 아니라서 필요).
- 기존 "선택 항목 XML"(체크된 것만)·"전체 CSV"(이미 있었음, 그대로 유지 — CSV는 원래도 파일마다 안 지워졌으면 병합됐을 텐데 업로드 리셋 버그 때문에 사실상 불가능했던 것도 같이 해결됨)는 그대로 유지.
- 실제 동작 검증: 파일 선택창은 자동화로 못 누르니, 페이지 안에서 JS로 `DataTransfer`를 이용해 가짜 File을 만들어 `input[type=file]`의 change 이벤트를 직접 발생시키는 방식으로 XML 2개를 순서대로 업로드 → `instancesData.length`가 2→3으로 누적되는 것, 각 인스턴스의 `timeline` 필드가 올바른 출처 파일명인 것, "2개 경기 누적됨" 배지, `exportToXML(false)`가 실제로 두 파일의 인스턴스를 모두 포함한 병합 XML(파일명 `병합_2개경기`)을 만드는 것, 기존 "선택 항목 XML"이 선택 없을 때 여전히 올바르게 막히는 것, "세션 초기화"가 완전히 비우는 것까지 전부 콘솔에서 직접 확인. 콘솔 에러 없음.
- 넓은 화면(1440px)에서는 헤더가 정상 렌더링됨. 이 도구는 원래도 좁은 화면 대응이 안 된 전문가용 데스크톱 툴이라(캔버스+비디오+타임라인 3분할), 모바일 대응은 이번 작업 범위 밖으로 판단.

✅ **완료 (2026-08-13): 태깅 → 대시보드 원본 XML 저장**
사용자 요청: 태깅한 내용이 원래 업로드했던 XML에도 반영돼서, 나중에 대시보드에서 그 경기 XML을 다시 뽑을 때도 반영되면 좋겠다. 단 한 경기씩만 — 한번에 여러 경기를 저장하면 위험.
- 태깅 도구가 대시보드와 같은 Firebase 프로젝트에 직접 연결(`public/Alter_sportsplay/firebase-config.js`와 동일한 설정을 `<script type="module">`로 인라인). 헤더 아래 새 바("대시보드에 저장")에 대회 선택 → 경기 선택 드롭다운(대시보드처럼 Firestore에서 실시간으로 불러옴) + "이 경기 원본 XML에 저장" 버튼.
- 저장 시 갱신되는 건 **그 경기 문서의 `rawSourceText`(원본 XML 텍스트)뿐** — `events`/`matchStats` 등 리포트 통계는 건드리지 않음(안전한 쪽으로, 사용자가 명시적으로 선택). 통계까지 반영하려면 기존 "교체" 업로드를 그 경기에 다시 쓰면 됨.
- **한 경기씩만**: 세션에 경기가 2개 이상 누적돼 있으면 저장 버튼이 막히고 안내 메시지가 뜸(먼저 "세션 초기화" 후 경기 하나만 업로드하도록 유도) — 병합 export 기능과는 별개로 완전히 분리된 안전장치.
- 병합 로직: 업로드했던 원본 XML을 다시 파싱해서, 관리 대상 그룹(Player/GK_Player/Type/Output/OUT Dir/X_Loc/Y_Loc/X_Goal/Y_Goal/커스텀필드)의 라벨만 지금 태깅한 값으로 갈아끼우고, 그 외 원본 라벨(컨텍스트 등)은 그대로 둠 — 인스턴스는 원본의 `<ID>`로 정확히 매칭.
- **부수 수정**: `parseSportscodeXML`(XML 업로드 파싱)이 원래 `GK_Player`와 커스텀필드를 안 읽어들이던 걸 발견해서 같이 고침(JSON 업로드 경로는 이미 읽고 있었음) — 안 고쳤으면 XML을 태깅 → 저장 → 나중에 그 XML 다시 불러와서 이어서 태깅할 때 GK_Player/커스텀필드가 매번 사라지는 문제가 있었을 것.
- 실제 검증(전부 실 Firestore, 임시 대회로): 메인 대시보드에서 테스트 XML 업로드→저장(React 파일 인풋이 합성 change 이벤트를 안 받아들여서, `__reactProps$` 키로 React onChange 핸들러를 직접 호출하는 방식으로 우회) → 태깅 도구에서 같은 대회/경기를 드롭다운으로 선택 → 원본 XML 재업로드 → 인스턴스 2개 중 하나는 새 값으로 변경, 하나는 GK_Player+좌표 추가 → 저장 → Firestore 문서를 직접 `getDoc`으로 읽어서 병합된 XML 텍스트를 확인: 관리 대상 라벨은 정확히 갱신되고, 원본에 있던 비관리 라벨(Context)은 그대로 보존된 것까지 확인. 이후 테스트 대회/경기를 Firestore에서 직접 삭제하고 대회 개수가 원래 9개로 돌아온 것까지 재확인.

## Phase 5 — 클라우드 저장소 이원화
- ✅ 유튜브 다중앵글 입력 (영상 연결 다이얼로그로 커버)
- ❌ 구글 드라이브 연동 (OAuth 필요, 큰 작업)
- ❌ 필터 재생목록

## Phase 6 — 대회 허브 (진행 중)
- ✅ Pool 순위표 (승점/득실차 자동계산, 대회 관리 화면에 표시)
- ✅ Final standings (다단계 포맷 대진표) — 스테이지 라벨→순위 매핑 UI로 사용자가 직접 지정
- ✅ 참조 자동 치환 (Winner N/Loser N/Nth Pool X → 실제 팀명, 다단계 연쇄 참조 지원)
- ✅ 일정표 붙여넣기 → 자동 파싱 (TMS Match Listing 형식, 확정팀/Winner-Loser/Pool참조/스코어/상태 인식)
- ✅ 미입력 경기 슬롯 (일정표에서 바로 업로드+매치번호 연결)

✅ **완료 (2026-08-13): 참조 자동 치환 + 최종 순위**
- `src/lib/schedule-resolver.ts` 신규 — `resolveScheduleRefs`, `computePoolStandings`, `computeFinalStandings`, `parseScore` 순수함수.
  - Winner N/Loser N: 그 matchNumber에 연결된 실제 매치가 있으면 그 결과 최우선, 없으면 일정표 자체의 스코어 텍스트 사용(승부치기 "(N - M SO)" 표기 파싱 포함). 다단계 연쇄 참조(Final이 "Winner SF1"을 참조하는 식)도 재귀+순환가드로 지원.
  - Nth Pool X: 스테이지 라벨이 알파벳 한 글자인 경기만 모아 승3/무1/패0 + 득실차 순위표 계산 후 N위 팀명 치환.
  - Pool 참조와 Winner/Loser 참조가 한 경기 안에 섞여도(예: "Winner 5 v 2nd Pool A") 정상 해석됨.
- `MatchData.matchNumber` 필드 추가 — 등록된 경기 테이블에 "일정#" 열/연결 버튼으로 사용자가 직접 매치 번호를 지정해 실제 업로드 결과와 일정표를 연결.
- `Tournament.finalStandingsRules` 필드 추가 — "최종순위 규칙" 다이얼로그에서 스테이지 라벨별 승자/패자 순위를 사용자가 지정(예: Final→1/2위, "3/4"→3/4위, SF는 비워두면 최종순위 계산에서 제외).
- 대회 관리 화면에 Pool별 순위표 카드(복수), 최종 순위 카드 신규 추가. 일정표 테이블은 참조 대신 해석된 실제 팀명을 표시(미해석 항목은 계속 옅게 표시).
- 실제 앱(로컬 서버, 실 Firestore)에서 임시 테스트 대회로 전체 체인(Pool 순위표 → SF 승부치기 → Final/3/4 참조 → 최종 순위) 검증 완료 후 테스트 대회 삭제로 정리함.

✅ **완료 (2026-08-13): 미입력 경기 슬롯**
사용자 정의(원 기획서 원문): "미입력 경기 슬롯 (업로드 전 대기 상태)". 일정표에 팀은 확정됐지만(참조가 다 풀렸지만) 아직 실제 XML/CSV가 업로드 안 된 행을 표시하고, 그 자리에서 바로 업로드할 수 있게 함.
- 일정표 카드 헤더에 완료/대기중/미정 진행 현황 요약 추가.
- 일정표 각 행에 "데이터" 열 추가: 이미 업로드+연결된 매치번호면 "완료", 팀은 확정됐는데 아직 안 올라왔으면 "대기중"(클릭하면 그 자리에서 파일 선택 → 업로드 → 자동으로 그 matchNumber에 연결까지 한번에), 참조가 아직 안 풀렸으면 "미정".
- `TournamentService.addMatchToTournament`이 이제 매치 생성 시 `matchNumber`를 같이 저장할 수 있고 새 문서 id를 반환하도록 확장(기존 호출부(dashboard.tsx)는 영향 없음, 인자 옵셔널).
- 실제 앱에서 대기중 배지가 팀 확정 행엔 뜨고, 참조 미해결 행("Winner 99"처럼 존재하지 않는 매치 참조)은 "미정"으로 빠지는 것까지 확인. 파일 업로드 자체(브라우저 네이티브 파일선택창)는 이 세션 자동화 도구로는 실행까지 못 눌러봤지만, 기존에 검증된 "교체" 업로드 플로우와 동일한 파싱 경로를 그대로 재사용함.
- ✅ 대회 개요 직접 입력 / 일정표 인라인 편집(팀·순서 포함) / 등록된 경기 팀이름 수정+홈어웨이 스왑 (아래 상세)
- ⏸ FIH 랭킹 온디맨드 수집 — 사용자 요청으로 보류("천천히 해도 됨"), 대신 위 수동 편집 기능들로 대체

✅ **완료 (2026-08-13): 대회 개요 / 일정표 인라인 편집 / 등록 경기 팀 편집**
사용자 요청: FIH 랭킹 자동수집 대신 "대회 개요는 직접 적고, 일정도 직접 적거나 XML 넣을 때 팀이나 순서를 수정 가능하게" — 순서는 일정표상 순서 + 등록된 경기 목록 순서 + 홈/어웨이 순서 전부 포함.
- `Tournament.description` 필드 추가 — 대회 관리 화면 상단에 자유 텍스트로 직접 작성/수정(자동 수집 아님). `TournamentService.updateTournamentDescription`.
- 일정표 각 행을 인라인으로 수정 가능(Match#/일시/홈/어웨이/스테이지/스코어/상태/경기장 전부), 행 삭제, 위/아래 버튼으로 표시 순서 변경(matchNumber와 별개 — 파싱 직후엔 번호순이지만 이후엔 저장된 배열 순서가 곧 표시 순서). 전부 `saveSchedule` 공용 헬퍼로 `updateSchedule` 재저장.
- 등록된 경기 목록에 "팀 이름 수정"(오타 등 단순 이름 교체, 스탯 재계산 불필요라 `updateMatchTeamNames`로 필드만 갱신) / "홈·어웨이 바꾸기"(자리를 실제로 뒤바꾸는 거라, 이미 기록된 events로 `createMatchDataFromUpload`를 홈/어웨이 뒤집어서 재실행 → 스탯까지 정확히 재계산 후 `updateMatchData`로 저장) 버튼 추가.
- 실제 앱에서 임시 테스트 대회로 개요 저장 후 새로고침 유지 확인, 일정 행 스코어 수정, 순서 변경(위/아래), 행 삭제, 팀 이름 교체, 홈/어웨이 스왑까지 전부 실제 동작 확인 후 테스트 대회 삭제로 정리.

### 🔜 다음 작업 순서 (2026-08-13 사용자 지정)
1. ~~대회 개요 / 일정표 편집 / 팀 편집~~ ✅ 완료
2. ~~슈팅 결과 브레이크다운 + 모바일 컴팩트 뷰~~ ✅ 완료
3. ~~슈팅태깅도구 저장소 편입~~ ✅ 완료
4. ~~영상분석도구 + 슈팅태깅도구 + 모바일 컴팩트 뷰 색감/톤 통일~~ ✅ 완료(아래)
5. **다음 세션 시작 지점**: 간이 태깅도구(사용자 표현) 제작 — 사용자가 이 작업 다음으로 하겠다고 예고함, 구체 스펙은 아직 안 나옴(다음 세션에서 다시 물어볼 것)

✅ **완료 (2026-08-13): 영상분석도구 + 슈팅태깅도구 색감/톤 통일**
- `public/Alter_sportsplay/styles.css`의 `--accent`/`--accent-hover`를 메인 대시보드 primary 색상(`hsl(221 100% 50%)`)과 같은 색상(H221)으로 교체. 이 파일은 이미 모든 강조색이 `var(--accent)`를 참조하고 있어서 변수 2개만 바꿔도 전체에 일관되게 반영됨(JS 파일들도 grep해서 하드코딩된 강조색 없는 것 확인).
- `public/ShotTagging/index.html`: 헤더의 내보내기 버튼 5개가 rose/indigo/purple/emerald로 서로 의미 없이 제각각이던 걸 전부 파랑 계열로 통일(Tailwind blue-600/700이 마침 대시보드 primary와 같은 색상대) — "선택" 범위는 밝은 파랑, "전체/병합" 범위는 진한 파랑으로 옅음/짙음 차이만 둬서 오히려 정보량(범위)을 색으로 전달하게 함. 타임라인/비디오 업로드 아이콘(amber/sky)도 파랑으로 통일.
  - 손대지 않은 것: 폼의 그룹(Row)/GK Player/커스텀필드 색상 구분(sky/amber/purple)은 필드 종류를 한눈에 구별하려는 의도적 설계라 그대로 둠. "세션 누적" 배지(amber)는 대시보드의 "대기중" 배지와 같은 관례(주의 환기)라 유지. "대시보드에 저장" 관련 UI(emerald)는 대시보드의 PDF 내보내기·Alter_sportsplay의 성공 상태와 같은 관례라 유지, 파일 다운로드류와 구분되는 별도 색으로 남김.
- 모바일 컴팩트 뷰는 별도 추가 변경 없음 — 지난 세션에 이미 작업 완료된 상태 그대로, 색감 톤도 대시보드 자체 라이트 테마를 따르므로 재검토만 하고 문제 없음 확인.
- 실제 서버에서 두 도구 다 로드해서 스크린샷으로 직접 확인(콘솔 에러 없음).

## ✅ Phase 7 — 슈팅 존 그리드 맵 (진행 중, 1차 완료)

✅ **완료 (2026-08-14): 좌표 파싱 연결 + 슈팅 위치/골대 타겟 지도 + 슈팅 분석 페이지**
- 그동안 막혀있던 지점(태깅 도구가 X_Loc/Y_Loc/X_Goal/Y_Goal을 XML로 내보내도 대시보드 `parseXMLData`가 이름만 읽고 좌표는 버렸음) 해결. `MatchEvent`에 `xLoc/yLoc/xGoal/yGoal/outDir/shotType/shotOutput` 필드 추가하고 `src/lib/parser.ts`의 XML 파서가 태깅 도구가 실제로 쓰는 그룹명(`X_Loc`/`Y_Loc`/`X_Goal`/`Y_Goal`/`Type`/`Output`/`OUT Dir` — 태깅 도구 소스의 `MANAGED_XML_GROUPS`와 정확히 대조 확인)을 읽도록 확장. 부수 효과: `Output` 그룹이 이제 `resultLabel`에도 폴백으로 반영돼서, 슈팅 결과 브레이크다운(Phase 2)의 "결과 미기록" 비중도 앞으로 줄어들 것으로 예상(태깅 도구로 새로 좌표까지 찍은 경기부터).
- `src/components/shot-zone-map.tsx` 신규 — 재사용 가능한 필드(550x230cm)/골대(366x214cm) SVG 지도. 태깅 도구 자체 좌표계와 1:1로 맞춰서 캔버스 픽셀 스케일링 없이 viewBox로 그대로 그림(태깅 도구 소스의 D자 슈팅서클 caculation 그대로 이식). 팀 A=원형/팀 B=마름모, 결과별 색상(득점=에메랄드/GK선방=하늘/블락=보라/아웃=주황/실패=빨강 — 사용자가 준 참고 HTML의 색상 그대로 재사용), 마우스오버 툴팁, `구역 그리드` 토글(필드 4x3 + 골대 3x3, 셀별 득점/시도 라벨+히트맵 음영).
- `src/components/shot-analysis-dashboard.tsx` 신규 — 새 상단 탭 "슈팅 분석"(대시보드 nav에 추가). 대회 선택(전체/개별) → 팀 선택 → 그 팀이 관여한 모든 경기의 슈팅/PC 시도를 누적해서 지도+KPI(시도/득점/득점률/상대 GK선방+블락)+선수별 슈팅 통계 표로 표시.
- `src/components/dashboard.tsx`의 경기별 리포트(단일 경기 뷰)에도 같은 지도를 슈팅 결과 브레이크다운 바로 아래 삽입 — 그 경기 안에서 홈/어웨이 슈팅 위치·골대 타겟을 바로 볼 수 있음.
- **검증**: 실제 서비스엔 아직 좌표가 찍힌 슈팅 데이터가 없어서(이 세션에서 처음 연결한 기능이라, 태깅 도구로 좌표까지 찍어서 대시보드에 "교체" 업로드한 실 데이터가 아직 없음 — `tsc`로 타입은 확인했지만 실 데이터로는 못 함), 태깅 도구가 실제로 내보내는 XML 스키마와 정확히 같은 구조(그룹명·좌표 단위 전부 태깅 도구 소스 코드에서 직접 확인한 값)로 합성 XML을 만들어 실제 업로드 파이프라인(React 파일 인풋 우회 트릭)에 태워서 렌더링 확인 — 필드 지도(슈팅서클 정확히 그려짐, 3개 원형+1개 마름모가 올바른 위치·색상), 골대 지도(프레임/네트/보드+OUT LEFT가 프레임 밖 왼쪽에 정확히 표시), 그리드 토글(셀별 0/1, 1/2 등 득점/시도 라벨 정확) 전부 확인. 이 테스트는 Firestore 저장(대회 선택+저장 버튼) 없이 클라이언트 파싱·렌더링만 거쳤음을 재확인(저장 안 했으므로 정리할 테스트 데이터도 없음).
- **다음에 실제로 필요한 것**: 실제 경기를 슈팅 태깅 도구로 좌표까지 찍고 "대시보드에 저장"(rawSourceText만 갱신) 또는 "교체" 업로드(전체 재파싱, events까지 갱신)로 반영해야 진짜 데이터가 지도에 뜸 — rawSourceText 저장 경로는 events를 안 건드리므로, 좌표가 지도에 보이려면 "교체" 업로드가 필요하다는 점을 사용자에게 안내할 것.

✅ **완료 (2026-08-14, 같은 세션): 상대 전적 (Head-to-Head)**
- 사용자가 "슈팅 발사위치 그리드는 내가 조절해줘야할듯 이건 나중에 얘기하기로하고 다음껄로 가자"라고 해서 그리드 셀 크기/개수 튜닝은 보류, 원래 요청 목록의 다음 항목(상대 전적)으로 진행.
- `src/components/head-to-head-dashboard.tsx` 신규 + 대시보드 상단 "상대 전적" 탭 추가. 좌/우 팀 선택(교체 버튼 포함) → **역대 전적**(항상 전체 기간 기준, 승/무/패 + 통산 득점 + 경기당 평균 슈팅/PC — `tournament-manager.tsx`의 "국가별 보기"와 동일하게 `Tournament.startDate`로 연도 계산)과, 그 아래 **경기 목록**(대회/연도 다중선택 필터 — 필터는 목록에만 적용되고 역대 전적 카드는 영향 안 받음, 명세대로).
- 실 데이터로 검증(일본 vs 한국, 18경기 실제 데이터): 6승 5무 7패, 통산 득점 24:25, 경기당 평균 슈팅/PC 정확히 계산됨. 대회 필터 칩 클릭 시 목록만 18→1로 좁혀지고 역대 전적 카드는 그대로인 것 확인. 좌우 교체 버튼도 정확히 스왑 확인(0:4 ↔ 4:0 등). 콘솔 에러 없음(새 탭에서 재확인 — 기존 탭에 있던 400 에러 로그는 이 세션 초반부터 있던 캐시된 것으로 이 기능과 무관함을 확인).

✅ **완료 (2026-08-14, 같은 세션): H2H 개선(경기 클릭→리포트 이동, 평균지표 다양화, 대회목록 연도별 팝오버) + TMS 라인업 붙여넣기 + 선수 커리어 조회**
사용자 요청 원문 요약: (1) H2H 경기 목록 클릭 시 해당 경기 리포트로 이동 (2) 역대 전적은 그대로 두고 경기당 평균 지표를 더 다양하게 (3) TMS 라인업 붙여넣기 → 경기 리포트에 표시 (4) 경기 리포트 라인업에서 선수 클릭 → 그 선수가 뛴 대회/경기 목록 → 클릭하면 해당 경기 리포트로 (5) H2H 대회목록 표시를 클릭식 드롭다운으로, 연도별로 묶고 미분류는 미분류끼리. 득점/슈팅/선방 집계 + 영상 클릭연결과, 팀 먼저 고르는 "팀별 분석" 브라우징 화면은 사용자가 스스로 "나중에"라고 명시해서 이번엔 제외.

- **H2H 경기 클릭→리포트**: `head-to-head-dashboard.tsx`에 `onViewMatch` prop 추가, 경기 목록 행에 연결(`dashboard.tsx`가 기존 `handleViewMatchFromDB`를 그대로 전달). 실 데이터로 확인(일본 vs 한국 목록에서 한 행 클릭 → 그 경기 리포트로 정상 이동).
- **H2H 평균지표 다양화**: 슈팅/PC뿐이던 것을 득점·슈팅·PC·서클 진입·점유율·PC 성공률 6개 지표로 확장(전부 `TeamMatchStats`에 이미 있는 필드 재사용, 새 집계 로직 불필요). 역대 전적(승/무/패, 통산 득점) 카드는 그대로 유지.
- **H2H 대회목록 → 연도별 팝오버**: 기존에 flat하게 나열되던 대회 칩 버튼들을 `Popover` 트리거("대회" 버튼, 선택 개수 뱃지)로 바꾸고, 안에서 `Tournament.startDate` 연도별로 그룹(최신 연도 먼저)+헤딩 표시, 연도를 못 찾은 대회는 "미분류" 그룹으로 따로 모음(tournament-manager.tsx 국가별 보기와 동일한 그룹핑 규칙 재사용).
- **TMS 라인업 붙여넣기**: `src/lib/lineup-parser.ts`(신규) — 탭 구분 TMS 표를 파싱. 팀 이름 줄(탭 없는 한 줄)로 블록을 나누고, 선수 행(번호/이름(C)/(GK) 마커/Min/1st~4th 원문 보존 — "1st~4th" 칸의 정확한 의미는 TMS 문서 없이 확정 불가라 임의 해석 안 하고 원문만 보존), COACH/MANAGER 행을 처리. `MatchData.lineups?: {home?, away?}` 필드 추가(`types.ts`), `TournamentService.updateMatchLineups` 신규. `src/components/lineup-section.tsx`(신규) — 경기 리포트에 붙여넣기→파싱→팀 배정(select)→저장 UI, 저장된 라인업은 두 팀 표로 렌더(주장 왕관/GK 방패 아이콘, 코치·매니저 표시), 선수 이름 클릭 가능.
- **선수 커리어 조회**: `src/components/player-record-dialog.tsx`(신규) — 별도 선수 로스터 컬렉션 없이, 이미 저장된 모든 경기의 `lineups.home/away.players`를 이름으로 검색(대소문자·공백 무시 매칭)해서 그 선수가 뛴 경기 목록(대회명·연도·소속팀뱃지·주장/GK 여부)을 보여주고, 클릭하면 그 경기 리포트로 이동(다이얼로그 닫힘). 매치 리포트의 라인업에서 선수 클릭 시 열림.
- **검증**: 실제 매치(`M18_5v6_한국vs일본`)에 사용자가 준 예시 그대로(Italy 18명/Austria 17명, 주장·GK 마커, COACH/MANAGER 포함) 붙여넣기→파싱→홈(한국)/어웨이(일본) 배정→저장까지 실 Firestore에 반영 확인(직접 `getDocs`로 재확인: `lineups.home.players.length===18`, `away===17`). 저장된 라인업 표에서 주장(PUGLISI Sara/CZECH Johanna) 왕관, GK(CARUSO Lucia Ines/LENDL Elena/PASTOR Maria/STREB Michaela) 방패 아이콘 정상 표시. 라인업에서 "PASTOR Maria" 클릭 → 다이얼로그에 그 경기(M18_5v6_한국vs일본, 2026 여자�팀 네이션스컵 뉴질랜드·2026년, 한국 소속) 정확히 표시 → 클릭해서 같은 리포트로 이동 확인. (참고: 저장 직후 몇 초 이내에 곧바로 다이얼로그를 열면 Firestore 실시간 리스너 동기화 지연으로 일시적으로 "경기 없음"이 뜰 수 있음을 재현 확인 — 곧바로 다시 열면 정상 표시되는 걸로 봐서 코드 버그가 아니라 실시간 동기화 지연, 별도 조치 안 함.) 콘솔 에러 없음(새 탭에서 재확인). **검증에 쓴 실제 경기(`shP4fMX66tMTUYXDhUUJ`, 한국vs일본)는 실제 경기라 이탈리아/오스트리아 가짜 라인업이 남으면 안 돼서, 검증 직후 `lineups` 필드를 `deleteField()`로 완전히 제거해 원래 상태로 되돌림(직접 `getDoc`으로 `hasLineups: false` 재확인).**

✅ **완료 (2026-08-14, 같은 세션): 슈팅 위치·골대 타겟 홈/어웨이 완전 분리**
사용자 피드백: "슈팅위치 골대타겟 이거 홈 어웨이 서로 따로 표시해줘. 같이 두면 공존하게하지말고" — 한 지도에 홈/어웨이를 원형/마름모로 구분해 겹쳐 그리던 걸 완전히 분리된 패널로 재설계.
- `shot-zone-map.tsx` 리팩터: `SidePanel` 서브컴포넌트로 필드+골대+그리드+범례를 한 팀분만 그리도록 캡슐화, `ShotZoneMap`은 이제 `SidePanel`을 두 번(홈, 구분선, 어웨이) 렌더링. 한 지도에 두 팀이 안 섞이니 원형/마름모 구분이 필요 없어져서 전부 원형으로 통일하고 팀 색은 테두리로만 표시(그리드 히트맵 색상도 팀 색 기준으로 변경). 각 패널 하단에 그 팀만의 결과별 개수 요약(예: "득점 1 · GK 선방 1 · 블락 1") 추가.
- 데이터 헬퍼(`isShotAttemptCode`, `normalizeShotOutput`)와 `ShotDatum` 타입, 호출부(`dashboard.tsx`/`shot-analysis-dashboard.tsx`)는 변경 없음 — 인터페이스 그대로 유지하고 렌더링만 바꿔서 회귀 위험 최소화.
- 검증: 합성 XML(테스트A/테스트B, 슈팅+PC+OUT 섞어서)로 실제 업로드 파이프라인을 통해 확인 — 테스트A 패널(3건)과 테스트B 패널(2건)이 서로 완전히 분리된 섹션으로 렌더링, 구분선으로 나뉨, 그리드 토글도 각 패널 독립적으로 정상 동작(같은 인덱스라도 팀별로 다른 카운트). 슈팅 분석 탭에서도 동일 컴포넌트가 정상 렌더링 확인. 콘솔 에러 없음(새 탭 재확인).

✅ **완료 (2026-08-14, 같은 세션): 슈팅 위치·골대 타겟 좌 홈 / 우 어웨이 레이아웃 + 크기 축소**
사용자 피드백: "슈팅위치 타겟이거 너무 커 좌홈 우어웨이로 크기 잘 맞춰서 넣어줘" — 분리는 됐지만 위아래로 쌓여서 너무 컸던 걸 좌/우 2열로 재배치.
- `ShotZoneMap` 컨테이너: `grid-cols-1 lg:grid-cols-2`(+`lg:divide-x`)로 변경해서 데스크톱(≥1024px)에서 홈 왼쪽/어웨이 오른쪽 2열, 세로 구분선으로 나뉘게 함(좁은 화면에선 기존처럼 세로로 쌓임).
- `SidePanel` 내부도 필드/골대를 좌우(그리드 7:5)가 아니라 위아래로 쌓고 `max-w-sm`으로 개별 지도 최대폭을 제한 — 2열이 되면서 패널 폭이 절반으로 줄어드는데, 필드+골대를 계속 좌우로 두면 각 지도가 너무 좁아져서 위아래 배치로 바꿈.
- 검증: 브라우저를 1280px 폭으로 리사이즈 후 확인 — 좌 테스트A(파랑)/우 테스트B(빨강) 2열, 세로 구분선, 각 지도 크기가 이전보다 훨씬 작고 적절함. 그리드 토글도 양쪽 패널에서 독립적으로 정상 동작(파랑/빨강 히트맵). 800px 폭에서는 기존처럼 세로 스택으로 자연스럽게 전환됨. 콘솔 에러 없음(새 탭 재확인).

✅ **완료 (2026-08-14, 같은 세션): 지도 크기·중앙정렬 조정 + PC/필드슛 구분**
사용자 피드백: "살짝 세로로 더 길게 해줘 잘 안보이니 숫자가. 그리고 가로로도 중앙에 맞게 해줘 내말은 각각 좌우 나눈 그리드 중앙에 맞게. 이거 pc랑 논pc도 구별해주고"
- 크기/가독성: 개별 지도 폭 상한을 `max-w-sm`(384px)→`max-w-md`(448px)로 키우고, 그리드 셀 안 숫자 폰트(9→13)·슈팅 마커 반지름(5→6)을 키워서 실제로 더 잘 보이게 함(억지로 viewBox 비율을 늘려 여백을 넣는 방식은 시도해봤지만 의미 없는 빈 공간만 생겨서 되돌리고, 실제 콘텐츠 크기를 키우는 쪽으로 정리).
- 중앙정렬: 각 패널의 `mx-auto lg:mx-0`(데스크톱에서 좌측 정렬)를 `mx-auto`로 바꿔서 좌/우로 나뉜 각 칸 안에서 지도가 항상 가운데 오도록 수정.
- PC/필드슛 구분: `ShotDatum.isPC` 필드 추가, `isPcAttempt(code, shotType)` 헬퍼 신규(Type 라벨이 "PC"로 시작하거나 code에 "페널티코너"/"PC"가 있으면 true). 마커 모양을 원형(필드슛)/사각형(PC)으로 구분하는 `ShotMarker` 서브컴포넌트 추가, 헤더에 모양 범례("○ 필드슛 · ▢ PC") 추가, 툴팁에도 PC 표시 추가. 호출부(`dashboard.tsx`, `shot-analysis-dashboard.tsx`) 둘 다 `isPC: isPcAttempt(e.code, e.shotType)`로 채우도록 수정.
- 검증: 합성 XML(필드슛+PC 섞어서, 테스트A/B 각 1건씩 PC)로 확인 — DOM에서 `rect[rx]`(사각형) 개수가 팀별 PC 시도 수와 정확히 일치, 스크린샷 확대로 골대 타겟 지도에서 사각형(PC)과 원형(필드슛)이 실제로 다른 모양으로 렌더링되는 것 육안 확인. 콘솔 에러 없음(새 탭 재확인).

✅ **완료 (2026-08-14, 같은 세션): 팀별 분석 (팀 → 대회 → 경기 → 라인업 → 선수 → 리포트 진입점)**
사용자가 "다음꺼 진행하자"로 이어서 진행 — TMS 라인업 작업 때 설명했던 전체 동선 중 남아있던 "팀 먼저 고르는 진입점" 부분.
- `src/components/team-analysis-dashboard.tsx` 신규 + 대시보드 상단 "팀별 분석" 탭 추가. 팀 선택 → 그 팀이 뛴 대회 목록(연도별 그룹, `tournament-manager.tsx`/H2H와 동일한 그룹핑 규칙 — 연도 못 찾은 대회는 "미분류"로 따로 모음) → 대회 클릭 시 그 대회 안 이 팀의 경기 목록 펼쳐짐(라인업 저장 여부 뱃지 표시) → 라인업 있는 경기 클릭 시 그 자리에서 라인업 표시(라인업 렌더링은 `lineup-section.tsx`의 `LineupTable`을 export해서 재사용, 중복 구현 안 함) → "경기 리포트 보기" 버튼으로 이동.
- 라인업 안 선수 이름 클릭 시 기존 `PlayerRecordDialog`(선수 커리어 조회) 그대로 재사용 — `onPlayerClick` prop을 `dashboard.tsx`의 `setOpenPlayerName`으로 연결.
- **실 데이터 발견**: 검증 중 "M18_5v6_한국vs일본" 경기에 라인업 데이터가 남아있는 걸 발견 — 이전 세션에서 테스트 후 지웠다고 기록했던 바로 그 경기라 처음엔 정리 안 된 테스트 데이터인가 의심했으나, 직접 조회해보니 팀명이 "Korea"/"japan", 선수명도 실제 한국/일본 선수 이름(LEE Seoyeon, KIM Jeongihn, SUZUKI Miyu 등)이라 사용자가 기능 배포 후 직접 넣은 **진짜 실 데이터**로 확인 — 삭제하지 않고 그대로 둠(지우기 전에 반드시 내용 확인하는 습관이 실수를 막음).
- 검증: 실 데이터로 전체 동선 확인 — 한국 팀 선택 → "2026 여자팀 네이션스컵 뉴질랜드" 대회 펼침(10개 대회 중 하나, 미분류 대회 2개 정상 분리) → M18_5v6_한국vs일본 경기(라인업 뱃지 있음) 펼쳐서 실제 라인업 표시 → "LEE Seoyeon" 클릭 → 선수 다이얼로그에 그 경기 정확히 표시 → 클릭해서 실제 그 경기 리포트(한국vs일본, 양팀 라인업 포함)로 정상 이동 확인. 콘솔 에러 없음(새 탭 재확인).

✅ **완료 (2026-08-14, 같은 세션): 남/여 대회 혼합 방지 — 카테고리(성별) 스코프 필터**
사용자 피드백(긴급): "하키 경기나 대회 이거 무조건 남자 여자로 나뉘어져있는거임 묶여서 하면안됨 ㅜㅜ 내가 분류 다할테니 일단 안된건 미분류로 넘겨주고. 다 팀별분석에도 나눠줘, 상대전적도 여자 남자 나뉘는거야" — 같은 "한국"이라도 남자대표팀과 여자대표팀은 완전히 다른 팀인데, 상대 전적·팀별 분석·슈팅 분석이 전부 팀 이름만으로 대회를 가로질러 묶고 있어서 남녀 경기가 섞이는 실제 데이터 정확성 버그였음(디자인 문제가 아니라 실제 통계가 잘못 나오는 문제).
- 이미 있던 `Tournament.category` 필드(예: "여자대표팀")를 활용 — category 없는 대회는 전부 "미분류"로 묶음(임의로 남/여를 추측하지 않음, 사용자가 직접 분류할 예정이라고 명시).
- `head-to-head-dashboard.tsx`, `team-analysis-dashboard.tsx`, `shot-analysis-dashboard.tsx` 세 군데(팀 이름으로 대회를 가로지르는 화면 전부) 동일하게: 카테고리 선택 pill(tournament-manager.tsx 카테고리 필터와 동일한 UI 패턴) 추가 → 선택된 카테고리 안의 대회로 먼저 좁힌 뒤에야 팀 이름 목록/집계를 계산하도록 수정. `shot-analysis-dashboard.tsx`의 "전체 대회"도 이제 "선택한 카테고리 안의 전체 대회"를 뜻함(진짜 전체 컬렉션이 아님).
- 실 데이터로 확인한 현재 카테고리 분포: `여자대표팀`(3개 대회) / `미분류`(7개 대회, "일본시리즈_남자"·"2026 남자대표팀" 등 이름은 남자대회지만 category 필드가 아직 없어서 전부 미분류에 들어가 있음 — 사용자가 언급한 "내가 분류 다 할테니"에 해당).
- 검증: 3개 화면 전부에서 카테고리 pill 클릭 시 팀 목록이 실제로 바뀌는 것 확인(상대 전적: 여자대표팀→뉴질랜드/미국, 미분류→AWAY/HOME/한국/일본 등으로 팀 풀이 완전히 달라짐). 콘솔 에러 없음(새 탭 재확인 — 기존 탭에 남아있던 JSX 파싱 에러 로그는 편집 중간 상태의 캐시된 HMR 에러였고 `tsc --noEmit`은 이미 깨끗했음, 새 탭에서 무에러 재확인).

⚠️ **사고 (2026-08-14, 같은 세션): 이전 정리 작업에서 실 비디오 링크 데이터 삭제됨**
사용자가 "대시보드 통계에서 슈팅/PC 클릭 → 영상 이동이 원래 됐었는데 사라졌다"고 보고. 원인 확인: 이 세션 초반 Alter_sportsplay 아키텍처 정리 때, 실제로 비디오가 연결돼있던 유일한 2개 경기(일본시리즈_남자/20260417 한국vs일본, 2026 여자팀 네이션스컵 뉴질랜드/M01_인도vs미국)의 `Matches`(비디오 메타데이터, 카메라 URL 포함) 문서를 "예전 아키텍처 잔재"로 판단해 삭제하고 `videoMatchId`도 같이 비웠는데, 확인해보니 이게 실제 사용자가 입력해둔 진짜 영상 링크였음(직접 재확인: 현재 128개 경기 중 `videoMatchId` 있는 경기 0개, `Matches` 컬렉션도 완전히 빔). 클릭→영상 이동 코드 자체(`basic-match-stats.tsx`/`shot-breakdown.tsx`/`match-event-timeline.tsx`/`attack-threat-chart.tsx`)는 전부 정상 — `videoMatchId`가 있어야만 작동하는데 그 데이터가 없어진 것. 카메라 URL 텍스트 자체는 복구 불가(백업 없음) — 사용자가 다시 갖고 있으면 그 경기 "영상" 버튼으로 재입력하면 즉시 복구됨. 사용자가 "된다"고 확인 — 재입력해서 해결한 것으로 보임. **교훈**: "잔재로 보이는 데이터"라도 실제 값(URL 등)을 확인하기 전엔 지우지 말 것 — 이후 세션(팀별 분석 검증 때 발견한 실 라인업 데이터 건)에서 이 교훈을 적용해 삭제 전 내용 확인하는 습관으로 다행히 두 번째 실수는 막음.

✅ **완료 (2026-08-14, 같은 세션): 대회 관리에 XML 다운로드 버튼 추가 + 영상 다운로드 관련 안내**
사용자 질문: "대회관리에서 xml과 영상 다운받는거. 되게해야하는데" → 확인 결과 (1) XML 다운로드는 경기 리포트 안에는 이미 있었음(PDF 버튼 옆) — 대회 관리에는 없어서 추가 요청 (2) 영상 다운로드는 "내가 유튜브에 올리는 내 영상"을 다운받고 싶다는 것.
- `tournament-manager.tsx`의 "등록된 경기" 테이블 Actions 열에 "XML" 다운로드 버튼 추가(경기 리포트의 `handleDownloadRawXml`과 동일한 Blob 다운로드 로직 재사용) — `rawSourceText` 없는 경기는 비활성 스타일 + "저장된 원본 XML 없음" 툴팁, 클릭 시 안내 토스트.
- 영상 다운로드는 앱 안에 구현 안 함 — 이 앱의 유일한 "영상 추출" 기능(Alter_sportsplay, MediaRecorder+화면공유)은 자기 화면을 실시간으로 녹화하는 방식이라 배경/일괄 다운로드가 원천적으로 불가능하고, 유튜브 영상 자체를 프로그램으로 긁어오는 건(본인 소유 영상이라도) 유튜브 자체 API가 아니라 비공식 방식이 될 수밖에 없어서 만들지 않기로 함 — 대신 유튜브 스튜디오 자체의 "다운로드" 기능(본인이 올린 원본 영상은 유튜브 스튜디오에서 직접 다운로드 가능)을 안내함.
- 검증: 대회 관리 화면에서 실제 등록된 경기(일본시리즈_남자, 6경기)에 XML 버튼이 전부 렌더링되고, `rawSourceText` 없는 경기들에 정확히 비활성 상태+안내 문구가 뜨는 것 확인. 콘솔 에러 없음(새 탭 재확인).

✅ **완료 (2026-08-14, 같은 세션): 유튜브 링크 빠른 확인 팝오버**
사용자가 유튜브 다운로더 자체를 만들어달라고 했다가("비공식이어도 상관없어"), 계정 접근 권한까지 제안(거절 — 비밀번호/계정 접근은 사용자가 허락해도 절대 안 하는 규칙이라 설명) → 결국 "유튜브링크만 그러면 누르면 옆에 뜨게해줘 그러면 다운받기 쉬울테니" 로 정리. 링크 자체를 다운로드하는 게 아니라 이미 저장된 URL을 빠르게 꺼내 보여주기만 하는 거라 문제 없음.
- `src/components/video-links-popover.tsx` 신규 — `videoMatchId`가 있는 경기에 작은 링크 아이콘 버튼을 붙이고, 클릭하면 팝오버로 그 경기에 연결된 유튜브 URL들(전술캠1/전술캠2/중계 영상 중 값 있는 것만)을 각각 "새 탭에서 열기" 링크 + "복사" 버튼으로 보여줌. `VideoMatchService.get`으로 팝오버 열 때만 지연 로드(항상 미리 안 불러옴).
- `tournament-manager.tsx`(등록된 경기 Actions 열, XML 버튼 옆), `dashboard.tsx`(경기 리포트 헤더, 기존 "영상 분석 보기" 버튼 옆)에 배치.
- 검증: 임시 테스트 대회+경기를 만들어 실제 영상 연결 다이얼로그로 유튜브 URL 저장 → 링크 팝오버 열어서 "전술캠 1" + URL이 정확히 표시되는 것 DOM으로 확인. 테스트 대회/경기/비디오링크 문서 전부 삭제해서 정리 완료(직접 재조회로 3개 다 `exists:false` 확인). 콘솔 에러 없음.

❌ **아직 안 한 것 (사용자가 명시한 "나중에" 범위)**:
- 슈팅 발사위치 그리드 셀 크기/개수 조절 UI — 사용자가 직접 조절하고 싶다고 해서 보류
- 득점/슈팅/선방·블록을 선수별로 모아서 보여주고, 영상이 연결된 경기면 그 장면 영상 클릭 재생까지 연결

## ✅ 비디오 분석 도구(Alter_sportsplay) UI 정리
사용자 피드백: "영상 보는 alter_sportsplay여기서 영상 업로드 못하게 하고. 뷰어랑 익스플로러 양쪽에 있는사이드뷰 정신없어서 왼쪽 한쪽으로 몰아주고. 이거 영상보는거랑 아래쪽 그리는거 그리는거 깔끔하게 해줘. 다 작동하게는 해주고"

✅ **완료 (2026-08-14): 좌/우 분리 패널 통합 + 하단 그리기 툴바 정리 + 진짜 버그 발견/수정**
- **영상 업로드 관련**: 코드 전체(`app.js`/`player.js`/`library.js`/`index.html`)를 검색해봤지만 애초에 이 도구 안에 비디오 "파일"을 업로드하는 기능 자체가 없음(YouTube URL 연결은 Field Focus 대시보드에서만 하도록 이미 이전 세션에 정리됨). 새로 막을 게 없어서 별도 변경 없음 — 확인 결과를 사용자에게 안내.
- **좌우 패널 통합**: 예전엔 좌측 `Explorer`(Library/Admin&List)와 우측 `Viewer`(Scenes/Events)가 완전히 분리된 2개 패널이었음 → 왼쪽 패널 하나로 통합, 상단에 새 "Explorer ↔ Viewer" 섹션 전환 탭 추가. 안쪽 Library/Admin, Scenes/Events 탭은 ID 그대로 유지한 채 DOM만 이동시켜서 JS 로직(app.js/player.js/library.js)은 전혀 안 건드림. `#right-panel` 및 관련 토글 버튼(`toggle-right-btn`/`show-right-btn`)/CSS grid 우측 컬럼 전부 제거, 그리드를 `"left main"/"left bottom"` 2열 구조로 단순화.
- **하단 그리기 툴바 정리**: `.separator`가 CSS 정의 자체가 없어서 실제로는 안 보이는 빈 div였던 걸 발견(진짜 버그) — 실제 구분선으로 스타일링. 기본도구/도형도구/색상/속성/편집액션/저장·추출액션을 각각 `.tool-group`(옅은 배경 pill)으로 묶어서 한눈에 구역이 보이게 함. 아이콘 전용 버튼은 `.icon-btn`(정사각형 컴팩트)로, 텍스트 있는 버튼(저장/캡처/영상추출)은 기존 스타일 유지 — 기능/이벤트 핸들러는 전혀 안 건드림(클래스만 추가).
- **진짜 버그 발견 및 수정 (이 작업 중 우연히 발견, 사용자 요청과 직접 연관돼서 같이 고침)**: "패널 숨기기"(◄ 버튼) 토글이 실제로는 작동 안 했던 걸 발견 — 원인이 두 가지 겹쳐 있었음: (1) `.panel-header`/`.admin-section`/`.match-list`/`.event-list`에 남아있던 `min-width: var(--panel-width)`가 그리드 아이템의 콘텐츠 최소폭을 강제로 350px로 밀어올려서 `body.hide-left`의 `0px` 트랙 지정이 무력화됨(고전적인 flex/grid `min-width:auto` 함정 — `.panel`에 `min-width:0` 추가 + 저 3곳의 min-width 제거로 고침). (2) 원래 `body { transition: grid-template-columns 0.4s ... }`로 슬라이드 애니메이션을 걸어뒀었는데, `minmax()`가 섞인 grid-template-columns 값은 브라우저마다 보간이 잘 안 돼서 실제로 전환 자체가 멈춰버리는 문제가 있었음(직접 재현 확인: 클래스는 정상 토글되고 셀렉터도 매치되는데 실제 레이아웃은 안 바뀜) — transition 제거하고 즉시 전환되게 바꿔서 확실하게 작동하도록 고침.
- 검증: 새 탭에서 실제 버튼 클릭(`toggle-left-btn`/`show-left-btn`/`section-btn-explorer`/`section-btn-viewer`/`tab-btn-library`/`tab-btn-admin`/`tab-btn-scenes`/`tab-btn-events`)으로 전부 정상 전환 확인, 패널 숨기기 시 실제 렌더링 폭이 350px→~1px로 줄고 다시 펴면 350px로 복귀하는 것 `getBoundingClientRect()`로 확인, 모든 그리기 도구 버튼 존재 확인, `#right-panel`/관련 토글 버튼 완전히 제거된 것 확인. 콘솔 에러 없음(정적 리소스 전부 200 — 남은 400/404는 이 샌드박스에서 원래 간헐적인 Firestore 연결 이슈, 무관함).

---
작업 환경: 브랜치 `feature/phase0-readability`에서 작업 후 `origin`에 push, `main` 미접촉.
